package sds.classfile.constantpool;

import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.assertThat;
import static org.hamcrest.Matchers.is;

import static sds.classfile.constantpool.ConstantType.*;

public class ConstantInfoFactoryTest {
	private ConstantInfoFactory b;

	@Before
	public void set() {
//		this.b = new ConstantInfoFactory();
	}
	
	@Test
	public void testAttributeFactory() throws Exception {
//		assertThat(b.create(C_CLASS).getTag(), is(C_CLASS));
//		assertThat(b.create(C_DOUBLE).getTag(), is(C_DOUBLE));
//		assertThat(b.create(C_FIELDREF).getTag(), is(C_FIELDREF));
//		assertThat(b.create(C_FLOAT).getTag(), is(C_FLOAT));
//		assertThat(b.create(C_INTEGER).getTag(), is(C_INTEGER));
//		assertThat(b.create(C_INTERFACE_METHODREF).getTag(), is(C_INTERFACE_METHODREF));
//		assertThat(b.create(C_INVOKE_DYNAMIC).getTag(), is(C_INVOKE_DYNAMIC));
//		assertThat(b.create(C_LONG).getTag(), is(C_LONG));
//		assertThat(b.create(C_METHOD_HANDLE).getTag(), is(C_METHOD_HANDLE));
//		assertThat(b.create(C_METHOD_TYPE).getTag(), is(C_METHOD_TYPE));
//		assertThat(b.create(C_METHODREF).getTag(), is(C_METHODREF));
//		assertThat(b.create(C_NAME_AND_TYPE).getTag(), is(C_NAME_AND_TYPE));
//		assertThat(b.create(C_STRING).getTag(), is(C_STRING));
//		assertThat(b.create(C_UTF8).getTag(), is(C_UTF8));
	}
}